{
  "name": "switchboard",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": { "build": "node scripts/build.mjs", "pretest": "npm run build", "test": "node --test tests/*.test.mjs" }
}
